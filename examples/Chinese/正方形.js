/* ఇక్కడ మీ ప్రక్రియ లు వ్రాయ గలరు. ఉదాహరణ కి:    */

正方形 = ( 边 ) => {
  重复(4, () => {
    向前移动( 边 );
    向右转(90);
  });
}

演示 = () => {
  初始状态();
  隐藏笔刷();
  改变颜色为( 1 ); // 蓝色 
  _此处_ 边 = 100;
  _此处_ 颜色编号 = 0;
  当_时重复( () => 边 > 0, ()=> {
    正方形( 边 );
    向右转(36);
    边 = 边 - 10;
    颜色编号 = ( 颜色编号 + 1 ) % 16;
    改变颜色为( 颜色编号 );
  } );
}

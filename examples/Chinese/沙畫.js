
演示 = () => {
  初始状态();
    隐藏笔刷();
  重复(6, () => {
    改变颜色为( 1 );  // 蓝色 
    向右转(180);
    _此处_ 边 = 100;
    _此处_ 颜色编号 = 0;
    当_时重复( () => 边 > 0, ()=> {
  
      向右弯曲(边, 30 );
  
      向右转(30);
      边 = 边 - 10;
      颜色编号 = ( 颜色编号  + 15 ) % 16;
      改变颜色为( 颜色编号 );
    } );
  });

  向右转(310);
  抬起笔刷();
  向前移动(60 );
  放下笔刷()
  改变颜色为( 13 );
  实心圆(5 );
  圆形(10);
}
